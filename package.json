{
  "name": "db-explorer",
  "displayName": "DB Explorer",
  "description": "VS Code extension for Database browsing, queries, and data editing.",
  "version": "0.0.1",
  "publisher": "local",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Databases"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "postgres.connect",
        "title": "DB Explorer: Connect"
      },
      {
        "command": "postgres.disconnect",
        "title": "DB Explorer: Disconnect"
      },
      {
        "command": "postgres.refreshSchema",
        "title": "DB Explorer: Refresh Schema"
      },
      {
        "command": "postgres.runQuery",
        "title": "DB Explorer: Run Query"
      },
      {
        "command": "postgres.openTable",
        "title": "DB Explorer: Open Table"
      },
      {
        "command": "postgres.exportResults",
        "title": "DB Explorer: Export Results"
      },
      {
        "command": "postgres.clearPassword",
        "title": "DB Explorer: Clear Stored Password"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "postgres.runQuery",
          "when": "resourceLangId == sql",
          "group": "navigation@10"
        }
      ],
      "explorer/context": [
        {
          "command": "postgres.runQuery",
          "when": "resourceExtname == .sql",
          "group": "navigation@10"
        }
      ],
      "view/item/context": [
        {
          "command": "postgres.openTable",
          "when": "view == postgresSchema && viewItem == postgresTable",
          "group": "navigation@10"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "postgresExplorer",
          "title": "DB Explorer",
          "icon": "resources/postgres.svg"
        }
      ]
    },
    "views": {
      "postgresExplorer": [
        {
          "id": "postgresConnections",
          "name": "DB Connections"
        },
        {
          "id": "postgresSchema",
          "name": "DB Schema"
        }
      ]
    },
    "configuration": {
      "title": "DB Explorer",
      "properties": {
        "postgresExplorer.profiles": {
          "type": "array",
          "default": [],
          "description": "Saved PostgreSQL connection profiles.",
          "items": {
            "type": "object",
            "required": ["id", "label", "host", "port", "database", "user"],
            "properties": {
              "id": {
                "type": "string",
                "description": "Unique profile id."
              },
              "label": {
                "type": "string",
                "description": "Display name for the profile."
              },
              "host": {
                "type": "string",
                "description": "Host name or IP."
              },
              "port": {
                "type": "number",
                "description": "Port number."
              },
              "database": {
                "type": "string",
                "description": "Database name."
              },
              "user": {
                "type": "string",
                "description": "Database user."
              }
            }
          }
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p .",
    "test": "npm run compile && node ./dist/test/runTest.js",
    "watch": "tsc -watch -p .",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.30",
    "@types/pg": "^8.10.9",
    "@types/vscode": "^1.85.0",
    "@vscode/test-electron": "^2.3.8",
    "mocha": "^10.4.0",
    "typescript": "^5.4.2"
  },
  "dependencies": {
    "pg": "^8.11.5"
  }
}
